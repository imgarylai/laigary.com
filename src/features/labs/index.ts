// /labs — interactive demo pages for the npm packages in `@/lib/labs`.
// Only the page frame lives here; each playground is colocated with its route.
export { LabPage, LabIntro, LabField, LabOptions, LabCode } from "./LabPage";
