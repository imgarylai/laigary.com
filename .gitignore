node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.alchemy

# vitest coverage output
coverage/

# Ephemeral git worktrees created by agent tooling. Scratch checkouts, removed
# when the run ends — never project content. Anything deliberate under .claude/
# (settings, skills) can still be added explicitly.
.claude/worktrees/
